import { Story } from '@storybook/addon-docs';

# Avatar

<Story id="atoms-avatar--avatar" />

The avatar is the primary component that projects any image given a url, alt and dimensions.

The fallback image is provided as a static asset with default dimensions of the most common usage.
